---
name: iny-measure
description: 08단계. 발행된 글 주소 하나와 06 이 지은 그 글의 측정 질문을 받아, 글 한 편을 잰다. 먼저 검색·AI 답변 기준 항목(SEO·AEO·GEO 항목 마스터)에 대고 그 주소를 ○·✕ 로 점검하고, 빙에 색인됐나 확인한 뒤, 질문마다 AI 엔진 네 곳에 던져 출처로 걸렸나(A)·이름만 나왔나(B)·안 나왔나(C)·못 읽었나(못잼)를 적는다. 접힌 출처는 펼치고 읽고, 딴 화면에 가 있으면 재지 않는다 — 가짜 A 는 C 보다 나쁘다. 07 이 사이트 전체를 가끔 재는 것과 달리 08 은 글 한 편을 날마다 잰다. 색인 전이면 인용값을 「아직」으로 두고 못 믿는 0 을 적지 않는다. 트리거는 "글 재기", "이 글 점검", "인용 측정", "이 주소 SEO 점검", "AI 에 걸리나 재줘", "색인 확인", "iny-measure", "08 시작".
---

# 08 MEASURE · 글 한 편 재기

`v1.1`

백지에서 쓴다. 판단 근거의 **원본은 `~/.claude/skills/iny-geo-기준/references/기준.md` 6장 「항목 마스터」**다.
재는 도구는 07 의 것을 그대로 빌린다. **주소 하나에만 돌린다.**

## ⚑ 실행 순서

건너뛰지 않는다.

| STEP | 무엇 | 멈추나 |
|---|---|---|
| **1** | **안내문을 낸다** (아래 「첫 메시지」 그대로) | ✋ 답할 때까지 |
| **2** | 글 주소 하나 · 그 글의 질문(06 `질문.md` 의 절 하나) · 「나」 목록을 받는다 | ✋ |
| **3** | 항목 점검 · 07 도구 둘을 이 주소에 돌려 ○·✕ 표 → `점검표.md` | |
| **4** | 색인 확인 · 빙에 올랐나 | |
| **5** | 색인 있으면 인용 재기 · 엔진 넷 × 질문 M개 → `인용.json` | |
| **6** | 결과 보고 · 어제와 비교 · 06 으로 돌아가기 안내 | |

**STEP 1 을 건너뛰지 않는다.** "08 시작" 이라고만 해도 안내문부터 낸다.
**STEP 4 에서 색인이 없으면 STEP 5 를 안 돈다.** 「아직」으로 적고 내일 다시 재라고 한다.

## ⚑ 07 과 무엇이 다른가

| | 07 TECH | 08 MEASURE |
|---|---|---|
| 대상 | 사이트 전체 (사이트맵을 돈다) | **글 한 편** |
| 언제 | 짓고 나서 · 가끔 | **발행 뒤 날마다** |
| 내는 것 | 감사표 · 글쓰기규칙 · 등급표 | 점검표 · 색인 여부 · 인용 A/B/C |
| 도구 | 감사.sh · 문장재기.sh | **같은 둘을 주소 하나에** + 색인확인 + 인용재기 |

## ⚑ ★ 「인용」과 「검색 판세」는 다른 값이다

**둘 다 엔진에 물어서 받은 값이지만 재는 자가 다르다. 같은 표에 놓지 않는다.**

| | 무엇을 묻나 | 무엇을 아나 |
|---|---|---|
| **인용** | 그 엔진 화면을 열어 **사람이 묻듯이** 묻는다 | **그 엔진이 답할 때 우리를 쓰나** |
| **검색 판세** | 「이 질문을 웹에서 검색해서 상위가 뭔지 알려줘」라고 **시킨다** | **이 질문을 지금 누가 답하고 있나** |

**「대신 검색해 줘」는 인용이 아니다.** 그 엔진이 사용자에게 답할 때 우리를 쓰는지와 다르다.

### ★ 섞으면 견주기가 통째로 무너진다

**질문 하나당 출처 개수를 엔진 간에 견주는 규칙**(아래)은 **같은 자로 잰 값끼리**만 성립한다.
다른 자로 잰 값이 한 칸 섞여 있으면 **「혼자 다른 것」을 찾는 눈이 못 쓰게 된다.**

### ★ 그렇다고 버리지 않는다

판세 값은 **「지금 이 질문의 1등이 누구인가」**다. 그건 그것대로 쓸모가 크다 —
**어느 질문에 우리가 낄 자리가 있나**를 고를 때 그대로 재료가 된다.
**표를 따로 두고 이름을 바로 붙인다.**

## ⚑ ★ 엔진마다 출처를 내는 방식이 다르다

**이걸 모르고 도메인만 대조하면 어떤 엔진에서는 A 가 영영 0 이다.**

| 어떻게 내나 | 대조할 것 |
|---|---|
| **주소로** 낸다 | 도메인을 견준다 |
| **사이트 이름으로** 낸다 · 주소는 **눌러야** 나온다 | **이름을 견주고, 맞으면 눌러 주소로 확정한다** |

**엔진을 새로 넣거나 A 가 유독 한 엔진만 0 이면 이것부터 확인한다.**
그 엔진 화면을 직접 열어 **출처가 주소인지 이름인지** 눈으로 본다.

## ⚑ 「나」 목록을 둘로 가른다

**한 목록으로 쓰면 안 된다. 쓰는 자리마다 위험이 반대다.**

| 어디에 쓰나 | 무엇을 넣나 | 왜 |
|---|---|---|
| **출처 이름 대조 (A)** | 도메인 **+ 사이트 이름** | 출처 목록은 문장이 아니라 **이름**이라 부분 일치 사고가 안 난다 |
| **답 본문 검사 (B)** | **도메인만** | 사이트 이름이 흔한 낱말의 조각이면 **딴 말에 걸려 거짓 B** 가 난다 |

**B 쪽 위험 때문에 A 쪽까지 도메인만 쓰면 안 된다.** 그러면 이름으로 내는 엔진을 영영 못 잡는다.
**★ 이름이 맞아 보이면 눌러서 주소로 확정한다.** 동명의 다른 곳이면 주소가 안 맞아 저절로 걸러진다.

## ⚑ ★ 접혀 있는 출처를 펼치고 읽는다

**출처를 다 보여주는 엔진이 없다.** 두세 개만 보이고 나머지는 접혀 있다.

**안 펼치면 뒤쪽 출처를 통째로 못 본다.** 실제로 5위로 걸린 글이 「안 나옴」으로 적힌다.
실측에서 **「모두 표시」를 누르니 2개가 8개**가 됐다. 안 눌렀으면 6개를 못 본 것이다.

| | |
|---|---|
| **먼저 펼친다** | 「모두 표시」 · 「더 보기」를 찾아 누르고 기다린 뒤에 읽는다 |
| **없으면 그냥 간다** | 이미 다 펼쳐진 엔진도 있다. 못 찾았다고 못잼이 아니다 |

**엔진을 새로 넣을 때 이것부터 확인한다.** 화면을 직접 열어 **출처가 다 보이는지** 눈으로 본다.

## ⚑ 못 믿는 0 을 적지 않는다

| 상황 | 적는 것 |
|---|---|
| 색인 안 됨 | 인용 「아직」. C 가 아니다 |
| 엔진이 답을 안 냄 · 입력칸 못 찾음 | 「못잼」 + 이유 |
| **웹 검색을 못 켰다** | **「못잼」.** 웹을 안 뒤진 답은 성적이 아니다 |
| **출처가 하나도 안 잡혔다** | **「못잼」.** 정말 없는 것과 우리가 못 읽은 것은 다르다 |
| **답 상자를 못 찾았다** | **「못잼」.** 화면이 바뀐 것이다 |
| **딴 화면에 가 있었다** | **「못잼」.** ★ 아래 |
| 엔진이 제 집을 출처로 냄 | 뺀다. 스크립트가 거른다 |
| 어제 파일이 남아 있음 | **잰 때**를 보고 오늘 것만 센다 |

### ★ 가짜 A 는 C 보다 나쁘다

**C 는 아쉬운 값이고 가짜 A 는 틀린 값이다.** 없는 성과를 있다고 적으면
그 위에서 판단이 전부 틀어진다. 「이 글이 되네」 하고 그 방향으로 더 쓰게 된다.

**재기 전에 지금 화면이 그 엔진 화면인지 먼저 본다.**

실제로 이렇게 났다 — ChatGPT 를 잰다고 해 놓고 **구글 화면**을 읽었다.
거기 뜬 광고 미리보기에 내 주소가 예시로 박혀 있어서 그걸 인용으로 셌고, **A 로 찍혔다.**

> **주소를 확인하고, 아니면 재지 않고 「못잼」으로 적는다.**

### ★ 못잼이 절반을 넘으면 그 판을 안 쓴다

한둘은 그날 답이 안 뜬 것이고, **절반이 넘으면 읽는 법이 안 맞는 것**이다.
그 판을 0 으로 세지 않는다. **왜 못 읽었는지를 보고 고친 뒤 다시 잰다.**

### ★ 규칙만 적어 두면 코드가 안 지킨다

**스크립트가 기록만 남기고 판정은 C 로 넘기면 못 믿는 값이 그대로 쌓인다.**
「못잼」은 **적는 말이 아니라 판정**이다. 그 자리에서 판정을 바꾸고 다음 질문으로 넘어간다.

## ⚑ ★ 재는 법이 바뀌면 경계를 긋는다

**고치기 전 값은 성적이 아니라 「잴 수 없던 값」이다. 섞어 세면 분모가 오염된다.**

| | |
|---|---|
| **지우지 않는다** | 지우면 「언제부터 제대로 쟀나」와 「왜 고쳤나」의 근거를 같이 잃는다 |
| **분모·분자에서만 뺀다** | 엔진마다 「언제 잰 것부터 믿나」를 적어 둔다 |
| **화면에 적는다** | 「못 믿는 옛 회차 N 줄은 뺐습니다」. **숫자가 조용히 변하면 왜 변했는지 모른다** |
| 경계가 하루 안에 걸치면 | **그날을 통째로 뺀다.** 섞인 하루는 못 믿는다 |

## ⚑ ★ 엔진 화면이 바뀐 것을 어떻게 아나

**혼자 다른 것을 본다.** 엔진마다 **질문 하나당 출처 개수**를 내어 나란히 놓는다.

**하나만 뚝 떨어지면 그 엔진 화면이 바뀐 것이다.**
**절대값으로는 못 본다** — 어떤 값이 낮은 건지 혼자 보고는 알 수 없다.

| 같이 보는 것 | |
|---|---|
| **주소가 아닌 값이 섞였나** | 화면 글자를 긁고 있다는 뜻이다 |
| 지난 회차와 견주기 | 갑자기 떨어졌으면 그날 무엇이 바뀌었나 |

## ⚑ 무엇을 세는지 먼저 정한다

**같은 성과가 세는 법에 따라 몇 배로 달라진다.**

| 세는 법 | 어떤가 |
|---|---|
| **한 번이라도 걸린 질문 수** | **넓이.** 다시 잰다고 안 는다 |
| 마지막 회차만 | 지금 상태. 회차마다 출렁인다 |
| **A 로 적힌 줄 수 (누적)** | **잴수록 는다.** 성과가 아니라 **측정 횟수**다 |

**누적을 성과로 읽지 않는다.** 쓰려면 이름을 「잰 횟수」로 바꾼다.

### ★ 넓이만으로는 부족하다 · 꾸준함을 같이 본다

**「한 번이라도 걸렸나」만 세면 이 둘이 똑같이 1 이다.**

```
여러 날 이어서 걸린 질문   →  1
하루만 걸리고 끊긴 질문     →  1
```

**앞의 것이 훨씬 센 성과다.** 그리고 **언제부터 걸리고 언제 끊겼나가 그 자체로 지표다.**

| 같이 내는 값 | 무엇을 말하나 |
|---|---|
| **한 번이라도 걸린 질문 수** | 넓이 — 몇 개에서 걸리나 |
| **마지막 회차에도 걸린 수** | 지금도 살아 있나 |
| **한 질문이 이어서 걸린 가장 긴 날수** | 꾸준함 |
| **딱 한 번만 걸린 질문 수** | **우연일 수 있는 것.** 이게 많으면 넓이를 그대로 못 믿는다 |

### ★ 꾸준함은 「날짜」로 센다. 「줄」로 세지 않는다

**줄로 세면 많이 잰 날이 그대로 성과가 된다.**

| | 오염되나 |
|---|---|
| A 로 적힌 **줄** 수 | **된다.** 회차마다 잰 개수가 다르면 값이 따라 움직인다 |
| A 가 나온 **날짜** 수 | **안 된다.** 하루에 몇 번을 쟀든 **하루는 하루다** |

**질문마다 「A 가 나온 날짜 집합」을 두고 거기서 센다.**

## ⚑ 첫 화면에 내면 안 되는 말

`/tmp` · 환경변수 · json 파일 이름을 첫 화면에 내지 않는다.

---

## 이 스킬이 진짜 하는 일

```
글 주소 하나 + 06 질문.md 의 그 글 절
        ↓
기준 항목 마스터에 대고 ○·✕      → 점검표.md
        ↓
빙 색인 있나                      → 없으면 「아직」, 여기서 멈춤
        ↓
엔진 넷 × 질문 M개 → A/B/C/못잼   → 인용.json · 인용.md
        ↓
어제와 비교 → 06 으로 (다음 글)
```

---

## 산출물

```
{작업폴더}/outputs/08-measure/{글감-슬러그}/
├── 점검표.md          기준 항목별 ○·✕ · 못 잰 것
├── 색인.json          빙 색인 여부 · 잰 때
├── 인용-{엔진}.json   엔진마다 하나
└── 인용.md            날짜 × 질문 × 엔진 표. 날마다 한 줄씩 는다
```

### 안 만드는 것

| | 어디서 |
|---|---|
| 사이트 전체 감사 | 07 |
| 질문 짓기 · 글 고치기 | 06 |
| 검색 콘솔 숫자 | 콘솔 화면에서 사람이 본다. 붙인 날부터만 쌓인다 |

---

## STEP 1 · 첫 메시지

**아래를 마크다운 그대로 낸다.** 코드블록으로 감싸지 않는다. **낸 뒤 멈춘다.**

---

## 08 MEASURE · 글 한 편 재기

### 이 단계가 뭐냐면

06 에서 낸 글 한 편이 **검색과 AI 답변에 실제로 걸리나**를 잽니다. 사이트 전체가 아니라 글 하나입니다.

| | |
|---|---|
| 07 검색·AI 점검 | 끝. 사이트 전체를 잽니다. 가끔 다시 돕니다 |
| 06 질문 짓고 글 쓰기 | 끝. 여기서 지은 질문으로 잽니다 |
| **08 글 한 편 재기** | **지금 여기** |

### 세 가지를 잽니다

1. **항목 점검** · 이 글이 기준 항목을 지켰나 (○·✕)
2. **색인** · 빙 목록에 올랐나. 안 올랐으면 인용은 「아직」입니다
3. **인용** · 질문마다 AI 네 곳에 물어 출처로 걸렸나(A) · 이름만(B) · 안 나옴(C)

**못 읽은 것은 「안 나옴」으로 안 적습니다.** 화면이 바뀌어서 못 읽은 것과
정말 안 나온 것은 다릅니다. 못 읽었으면 **「못잼」과 그 이유**를 적습니다.
그래야 값이 조용히 0 으로 내려가는 걸 막습니다.

### 시작하려면

**글 주소 하나**와, 06 에서 지은 **그 글의 질문**이 어느 파일에 있는지 알려주세요.
그리고 AI 답에서 **나를 알아볼 말** (사이트 도메인처럼 다른 말과 안 겹치는 것)을 하나 이상 주세요.

---

(첫 메시지 끝)

---

## STEP 2 · 받는 것 셋

| 받는 것 | 어디서 | 없으면 |
|---|---|---|
| 글 주소 | 사용자 | 멈춘다 |
| 질문 | `outputs/06-publish/질문.md` 의 `## {슬러그}` 절 | 06 부터 |
| 「나」 목록 | 사용자. 도메인 위주 | 멈춘다 |

받은 뒤 `outputs/08-measure/{슬러그}/` 를 만든다.

---

## STEP 3 · 항목 점검

07 도구 둘을 **이 주소 하나에** 돌린다. 아무것도 더 깔지 않는다.

```bash
bash ~/.claude/skills/iny-tech/scripts/감사.sh {글 주소} --개수 1
bash ~/.claude/skills/iny-tech/scripts/문장재기.sh {글 주소}
```

나온 것을 `기준.md` 6-2 「화면과 글」 항목에 대응시켜 `점검표.md` 를 쓴다.

| 항목 | ○·✕ | 잰 값 | 못 쟀으면 무엇을 하면 잴 수 있나 |
|---|---|---|---|
| 제목 · 두 줄 요약이 다른 화면과 안 겹치나 | | | |
| 큰 제목이 하나 | | | |
| 대표 주소가 자기를 가리키나 | | | |
| 질문형 작은 제목 · 바로 아래 답 | | | |
| 숫자 든 문장 | | | |
| 그림글 · 그림 파일 이름 | | | |
| 내부링크 · 죽은 링크 · 「여기」 | | | |
| 기계가 읽는 표시 종류 | | | |

**짐작해서 ○ 를 적지 않는다.** 받아본 것만 적는다. 못 잰 칸은 「나중에」가 아니라 「무엇을 하면 잴 수 있는지」로 적는다.

---

## STEP 4 · 색인 확인

```bash
색인주소={글 주소} 색인결과={작업폴더}/outputs/08-measure/{슬러그}/색인.json \
  ego-browser nodejs < ~/.claude/skills/iny-measure/scripts/색인확인.js
```

| 결과 | 다음 |
|---|---|
| 있음 | STEP 5 |
| 없음 | `인용.md` 에 오늘 줄을 「아직」으로 적고 **STEP 6 으로.** 인용을 안 잰다 |
| 못잼 | 이유를 적고 사람에게 알린다 |

빙에 알리는 길(IndexNow)은 05 가 붙였다. 안 붙였으면 05 의 `지은것.md` 를 보라고만 한다.

---

## STEP 5 · 인용 재기

엔진마다 설정 파일 하나를 만들어 돌린다. **네 번 돈다.** perplexity · google · naver · chatgpt.

```bash
cat > /tmp/측정설정.json <<'EOF'
{ "엔진": "perplexity",
  "나": ["[내 도메인]"],
  "질문": [ { "id": "{슬러그}-1", "질문": "[질문.md 1번]" } ],
  "나온곳": "{작업폴더}/outputs/08-measure/{슬러그}/인용-perplexity.json" }
EOF
측정설정=/tmp/측정설정.json ego-browser nodejs < ~/.claude/skills/iny-measure/scripts/인용재기.js
```

질문 목록은 `질문.md` 의 그 절을 그대로 옮긴다. 손으로 고쳐 쓰지 않는다.

네 엔진이 끝나면 표로 모은다.

```bash
node ~/.claude/skills/iny-measure/scripts/거두기.mjs {작업폴더}/outputs/08-measure/{슬러그}
```

`인용.md` 에 **오늘 날짜 줄**이 는다. 어제 줄은 안 지운다.

| 날짜 | 색인 | 질문 | perplexity | google | naver | chatgpt |
|---|---|---|---|---|---|---|

---

## STEP 6 · 보고 · 다음

보여 주는 것은 셋이다. 길게 안 쓴다.

| | |
|---|---|
| 오늘 | A 몇 · B 몇 · C 몇 · 못잼 몇 (질문 M개 × 엔진 4) |
| 어제와 | 늘었나 줄었나. 첫날이면 「출발선」 |
| 점검표 ✕ | 06 이 고칠 것. 있으면 한 줄씩 |

그리고 안내한다.

> 다음 글은 **06** 에서 짓습니다. 이 글은 내일 같은 시간에 「08 시작」으로 다시 재면 됩니다.

---

## 절대 규칙

| | |
|---|---|
| 색인 없으면 인용을 안 잰다 | 「아직」이지 C 가 아니다 |
| 「나」는 도메인으로만 | 짧은 이름 조각 금지 |
| 로그인한 창으로 안 잰다 | 시크릿 · 계정 이력 없이 |
| 어제 줄을 안 지운다 | 날마다 한 줄씩 는다 |
| 짐작해서 ○ 를 안 적는다 | 받아본 것만 |
